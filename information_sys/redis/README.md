# Redis — план следующего стенда

Будущий стенд: Redis primary/replica, затем Sentinel для обнаружения отказа и
failover. Отдельно проверим, почему асинхронная репликация допускает потерю уже
подтверждённых записей. До реализации здесь нет запускаемой конфигурации.
