# MongoDB — план следующего стенда

Будущий стенд: replica set из трёх участников, выборы primary, write concern и
read preference. Используем ту же складскую предметную область, но отдельный
адаптер сервиса. До реализации каталог не содержит запускаемой конфигурации.
