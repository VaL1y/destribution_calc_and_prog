# Cassandra — план следующего стенда

Будущий стенд: три peer-ноды, replication factor и разные consistency levels
для чтения/записи. Это позволит сравнить встроенное управление репликами с
PostgreSQL + внешним HA-менеджером. Пока каталог является только roadmap.
